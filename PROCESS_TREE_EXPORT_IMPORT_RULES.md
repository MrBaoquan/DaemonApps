# 进程树导出导入规则文档

## 1. 整体逻辑

### 进程树结构
```
根节点（[ 进程树 ]）- 项目级别，始终选中，不可更改，用于显示项目名称
├── 二级节点A - 可选择（用户主动选择的导出对象）
│   └── 三级节点A1 - 自动随父节点选择
│   └── 三级节点A2
├── 二级节点B
│   └── 三级节点B1
└── ...
```

### 节点选择的特点
- **根节点**：始终被选中，不可取消，仅作为项目名称的容器
- **二级节点**：用户可直接选择或取消选择
- **三级及以下节点**：
  - 用户可直接选择某个三级节点
  - 此时其所有父节点会自动被选中（依赖链）
  - 取消选择三级节点时，如果它是父节点的唯一选中子节点，则父节点也自动取消选中

---

## 2. 导出流程

### 2.1 节点收集阶段
```
用户操作 → 选中节点树 → GetSelectedNodes() → 选中的二级节点
```

**GetSelectedNodes() 的规则**：
1. 遍历根节点的直接子节点（二级节点）
2. **只收集选中的二级节点**（不递归收集三级及以下）
3. **排除根节点**（`IsSuperRoot == true`）
4. 三级及以下节点自动作为二级节点的子树包含

**例示**：
- 用户选择三级节点 → 二级父节点自动被选中
- GetSelectedNodes 返回：[二级节点]（只包含二级，三级自动作为其子树）
- 导出结果：二级节点及其完整子树都被导出

### 2.2 程序收集阶段
```
GetProgramsFromNodes(selectedNodes) → 程序文件列表
```

**规则**：
1. 只检查 `selectedNodes` 中的节点（不递归）
2. 对每个节点检查其 `NodePath` 是否指向可执行文件
3. 如果文件存在，添加到导出列表
4. 如果文件不存在，跳过该节点

### 2.3 虚拟根节点创建
```
selectedNodes → 创建虚拟根节点 → 将selectedNodes作为子节点
```

**虚拟根节点特点**：
- `NodeId`: 新生成的GUID
- `MetaData.Name`: 项目名称（来自原始根节点或用户输入）
- `MetaData.Path`: 空字符串（虚拟节点，无实际程序文件）
- `Children`: selectedNodes 的深拷贝（路径转换为相对路径）

### 2.4 进程树序列化
```
虚拟根节点 + 子节点 → XML序列化 → treeview.xml
```

**输出格式**：单根节点的 `List<ProcessItem>`
```xml
<ArrayOfProcessItem>
  <ProcessItem>
    <!-- 虚拟根节点 -->
    <NodeId>abc123...</NodeId>
    <MetaData>
      <Name>项目名称</Name>
      <Path></Path>
    </MetaData>
    <Children>
      <!-- 选中的二级节点 -->
      <ProcessItem>
        <NodeId>node1</NodeId>
        <MetaData>
          <Name>二级节点A</Name>
          <Path>relpath/to/programA</Path>
        </MetaData>
        <Children>
          <!-- 三级节点及以下 -->
          ...
        </Children>
      </ProcessItem>
      <!-- 其他二级节点 -->
      ...
    </Children>
  </ProcessItem>
</ArrayOfProcessItem>
```

### 2.5 导出流程总结
| 阶段 | 输入 | 处理 | 输出 |
|------|------|------|------|
| 节点收集 | 进程树 | GetSelectedNodes只收集二级节点 | 选中的二级节点 |
| 程序收集 | 选中节点 | GetProgramsFromNodes检查文件 | 程序文件路表 |
| 树构建 | 选中节点 | 创建虚拟根包装 | 虚拟根+子节点 |
| 序列化 | 虚拟根 | XML序列化 | treeview.xml |
| 压缩 | 配置+程序+树 | ZIP打包 | .dkit包 |

---

## 3. 导入流程

### 3.1 包解析
```
.dkit包 → 解压 → 读取 treeview.xml → List<ProcessItem>
```

**预期内容**：单个虚拟根节点 + 其子节点

### 3.2 导入对话框显示
```
List<ProcessItem> → AvailableProcessTree → 树控件显示
```

**显示内容**：
- 虚拟根节点（项目名称）显示在顶层
- 虚拟根节点下的二级节点及其子树显示为子项
- 默认全选所有节点

**例**：
```
✓ [ 进程树 ]              <- 虚拟根，可取消以切换导入模式
  ✓ 二级节点A
    ✓ 三级节点A1
  ✓ 二级节点B
```

### 3.3 用户选择分析
```
用户操作 → 检查根节点是否选中 → 决定导入模式
```

**导入模式**：
- **清空模式**：根节点被选中
  - 清空现有进程树
  - 重建为导入的树结构
  - 同步项目名称（如果启用）
  
- **合并模式**：根节点未被选中
  - 保留现有进程树
  - 将导入的二级节点合并进去
  - 可能产生冲突（同名节点）

### 3.4 节点过滤
```
GetSelectedNodes(AvailableProcessTree) → 实际导入的节点
```

**FilteringRules**:
1. 识别虚拟根节点（`Parent == null` 且 `NodePath` 为空或文件不存在）
2. **跳过虚拟根节点本身**，递归收集其选中的子节点
3. 返回实际的二级+节点列表

**例**：
- 用户在导入对话框中选择：虚拟根✓、二级节点A✓、二级节点B✓
- GetSelectedNodes 返回：[二级节点A, 二级节点B]（不包含虚拟根）
- 导入时处理这两个节点和它们的子树

### 3.5 路径转换
```
相对路径 + 路径映射 → 目标系统的绝对路径
```

**处理步骤**：
1. 读取导出时保存的相对路径
2. 应用路径映射（如果用户自定义了应用目录）
3. 转换为当前系统的绝对路径

### 3.6 树合并/重建
```
选中的节点 → MergeProcessTreeAsync → 更新 treeview.xml
```

**清空模式**：
- 删除现有树
- 直接导入新树

**合并模式**：
- 查找现有树中的对应节点
- 如果存在则更新或跳过（取决于冲突策略）
- 如果不存在则添加

### 3.7 导入流程总结
| 阶段 | 输入 | 处理 | 输出 |
|------|------|------|------|
| 解压 | .dkit包 | ZIP解压 | 临时目录 |
| 树加载 | treeview.xml | XML反序列化 | 虚拟根+子节点 |
| 显示 | 虚拟根+子 | 树控件绑定 | UI显示 |
| 选择分析 | 用户选择 | 检查根节点 | 导入模式 |
| 节点过滤 | 选择的树 | GetSelectedNodes | 实际导入节点 |
| 路径转换 | 相对路径 | 应用映射 | 绝对路径 |
| 树合并 | 导入节点 | 清空/合并 | 新进程树 |

---

## 4. 关键规则总结

### 4.1 根节点处理
| 操作 | 根节点 | 行为 |
|------|--------|------|
| 导出 | 收集 | **排除** - 仅用于项目名称 |
| 导出 | 显示 | 变为虚拟根，包装导出的节点 |
| 导入 | 显示 | 显示虚拟根（项目名称） |
| 导入 | 决策 | 选中=清空模式，未选中=合并模式 |

### 4.2 依赖关系链
```
用户选择三级节点
  ↓
二级父节点自动选中（依赖链建立）
  ↓
GetSelectedNodes只返回二级节点
  ↓
导出虚拟根包含二级节点及其完整子树
```

### 4.3 节点匹配规则
- **程序节点**：`NodePath` 指向可执行文件（.exe等）
- **文件夹节点**：`NodePath` 为空或指向目录（仅在虚拟根）
- **虚拟节点**：根节点且路径为空

### 4.4 导出包内容
```
DaemonKit_Export_YYYYMMDD_HHMMSS.dkit
├── Configs/
│   ├── settings.xml        (配置，可选)
│   ├── schedule.xml        (计划任务，可选)
│   ├── treeview.xml        (虚拟根+选中节点树)
│   └── metadata.json       (包元数据)
├── Programs/
│   ├── 二级节点A/
│   │   └── ... (程序文件)
│   ├── 二级节点B/
│   │   └── ... (程序文件)
│   └── ...
```

### 4.5 常见问题排查

**Q: 导入时多出了不选中的节点？**
- A: 检查虚拟根节点的Children，确保只包含被选中导出的节点

**Q: 导出两个节点而用户只选一个？**
- A: 用户选择的节点可能有子节点，导致父节点因依赖被动选中

**Q: 根节点选中后无法取消？**
- A: 正常行为 - 根节点始终必须被选中

**Q: 导入后某些程序文件丢失？**
- A: 检查GetProgramsFromNodes是否正确识别了所有选中节点的可执行文件

---

## 5. 代码关键点

### ExportDialogViewModel
```csharp
GetSelectedNodes(ProcessTree)  // 排除根节点，收集所有选中的二级+节点
```

### ExportImportService
```csharp
GetProgramsFromNodes(selectedNodes)  // 从节点提取程序路径
CreateVirtualRoot(selectedNodes)     // 包装为虚拟根
```

### ImportDialogViewModel
```csharp
GetSelectedNodes(AvailableProcessTree)  // 跳过虚拟根，收集实际导入节点
bool isRootNodeSelected = rootNode?.IsSelected  // 决定导入模式
```

---

## 6. 验证清单

导出前检查：
- [ ] 只有用户想导出的节点被选中
- [ ] 因依赖被动选中的父节点也在预期内

导出后检查：
- [ ] 日志显示的导出节点数量正确
- [ ] 导出的程序文件数量正确
- [ ] 虚拟根节点名称是项目名称

导入前检查：
- [ ] 虚拟根节点显示在顶层
- [ ] 虚拟根下的节点是导出时的节点

导入后检查：
- [ ] 选中根节点：树被清空并重建
- [ ] 未选中根节点：新节点被合并
- [ ] 所有程序文件成功复制
- [ ] 进程树配置正确更新
