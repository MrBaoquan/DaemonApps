# DaemonKit 任务计划功能重构文档

## 概述

本文档详细说明了DaemonKit任务计划功能的重构内容，包括新的触发器类型、任务操作类型，以及如何使用新的任务调度系统。

## 核心组件

### 1. 新的触发器类型 (ScheduleTriggerType)

在 `DaemonKit/Core/ScheduleTaskEngine.cs` 中定义，支持以下触发器：

#### 1.1 每天指定时间 (Daily)
- **说明**: 每天在指定的时间执行一次任务
- **配置参数**: `DailyTime` (HH:mm:ss 格式)
- **使用场景**: 例如每天16:30关闭电脑

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "每日关机",
    Action = ScheduleTaskAction.ShutdownSystem,
    Trigger = ScheduleTriggerType.Daily,
    DailyTime = "16:30:00",
    Enabled = true
};
```

#### 1.2 每天首次启动后延迟X秒 (OncePerDayAfterStart)
- **说明**: 每天首次启动管家后，延迟X秒执行一次
- **配置参数**: `DelaySeconds` (延迟秒数)
- **使用场景**: 例如 "每天管家第一次启动后延迟10秒后重启一次电脑"

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "每日首次启动后重启",
    Action = ScheduleTaskAction.RestartSystem,
    Trigger = ScheduleTriggerType.OncePerDayAfterStart,
    DelaySeconds = 10,
    Enabled = true
};
```

#### 1.3 每次启动后延迟X秒 (EveryStartupAfterDelay)
- **说明**: 每次启动管家后，延迟X秒执行一次（不限制频率）
- **配置参数**: `DelaySeconds` (延迟秒数)
- **使用场景**: 例如 "每次管家启动后延迟5秒后重启一次进程树结点"

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "启动后重启进程树",
    Action = ScheduleTaskAction.RestartProcessTree,
    Trigger = ScheduleTriggerType.EveryStartupAfterDelay,
    DelaySeconds = 5,
    Enabled = true
};
```

#### 1.4 启动后每隔X秒循环执行 (IntervalAfterStartup)
- **说明**: 管家启动后，每隔X秒重复执行一次任务
- **配置参数**: `DelaySeconds` (间隔秒数)
- **使用场景**: 例如 "管家启动后，每隔5秒全屏截图一次"、"每隔60秒重启一次进程"

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "定时截图",
    Action = ScheduleTaskAction.TakeScreenshot,
    Trigger = ScheduleTriggerType.IntervalAfterStartup,
    DelaySeconds = 5,
    Enabled = true
};
```

### 2. 新的任务操作类型 (ScheduleTaskAction)

#### 2.1 StartProcess - 启动进程
```csharp
Action = ScheduleTaskAction.StartProcess
```

#### 2.2 StopProcess - 停止进程
```csharp
Action = ScheduleTaskAction.StopProcess
```

#### 2.3 RestartProcess - 重启进程
```csharp
Action = ScheduleTaskAction.RestartProcess
```

#### 2.4 RestartProcessTree - 重启进程树
```csharp
Action = ScheduleTaskAction.RestartProcessTree
```

#### 2.5 ShutdownSystem - 关闭电脑
```csharp
Action = ScheduleTaskAction.ShutdownSystem
```

#### 2.6 RestartSystem - 重启电脑
```csharp
Action = ScheduleTaskAction.RestartSystem
```

#### 2.7 TakeScreenshot - 全屏截图
自动保存到 `Screenshots` 文件夹，文件名格式: `screenshot_YYYY-MM-DD_HH-mm-ss.png`

```csharp
Action = ScheduleTaskAction.TakeScreenshot
```

## 数据结构

### ScheduleTaskConfig (任务配置)

```csharp
public class ScheduleTaskConfig : ReactiveObject
{
    /// <summary>任务名称</summary>
    public string Name { get; set; }

    /// <summary>任务操作类型</summary>
    public ScheduleTaskAction Action { get; set; }

    /// <summary>触发器类型</summary>
    public ScheduleTriggerType Trigger { get; set; }

    /// <summary>每天执行的时间 (HH:mm:ss格式)</summary>
    public string DailyTime { get; set; }

    /// <summary>延迟或间隔时间(秒)</summary>
    public int DelaySeconds { get; set; }

    /// <summary>是否启用</summary>
    public bool Enabled { get; set; }

    /// <summary>备注信息</summary>
    public string Description { get; set; }
}
```

### ProcessItem 扩展

在ProcessItem中添加了新的属性用于存储任务配置：

```csharp
/// <summary>新的计划任务配置列表</summary>
public List<ScheduleTaskConfig> ScheduleTaskConfigs { get; set; }
```

## 任务调度引擎 (ScheduleTaskEngine)

### 核心功能

- **自动检测**: 每秒检查一次是否有任务需要执行
- **触发器管理**: 根据不同的触发器类型判断任务是否应该执行
- **状态追踪**: 记录任务最后执行时间，防止重复执行
- **事件通知**: 提供任务执行前和执行后的事件回调

### 集成方式

```csharp
// 在MainWindow中初始化
_scheduleTaskEngine = new ScheduleTaskEngine(rootProcessNode);

// 监听任务执行事件
_scheduleTaskEngine.TaskExecuting += (sender, context) =>
{
    NLogger.Info($"执行任务: [{context.TaskConfig.Name}]");
};

_scheduleTaskEngine.TaskExecuted += (sender, context) =>
{
    if (context.IsSuccess)
    {
        NLogger.Info($"任务完成: {context.Result}");
    }
};

// 在主循环中调用
_scheduleTaskEngine.CheckAndExecutePendingTasks();
```

## 配置示例

### 示例1: 每天16:30关闭电脑

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "每日16:30关机",
    Action = ScheduleTaskAction.ShutdownSystem,
    Trigger = ScheduleTriggerType.Daily,
    DailyTime = "16:30:00",
    Enabled = true,
    Description = "每天下午16:30自动关闭电脑"
};

processNode.ScheduleTaskConfigs.Add(taskConfig);
```

### 示例2: 启动后每隔5秒截图

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "定时截图",
    Action = ScheduleTaskAction.TakeScreenshot,
    Trigger = ScheduleTriggerType.IntervalAfterStartup,
    DelaySeconds = 5,
    Enabled = true,
    Description = "启动后每隔5秒全屏截图一次，保存到Screenshots文件夹"
};

processNode.ScheduleTaskConfigs.Add(taskConfig);
```

### 示例3: 启动后延迟10秒重启电脑

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "启动后重启",
    Action = ScheduleTaskAction.RestartSystem,
    Trigger = ScheduleTriggerType.OncePerDayAfterStart,
    DelaySeconds = 10,
    Enabled = true,
    Description = "每天首次启动后延迟10秒后重启电脑"
};

processNode.ScheduleTaskConfigs.Add(taskConfig);
```

### 示例4: 启动后5秒重启进程树

```csharp
var taskConfig = new ScheduleTaskConfig
{
    Name = "启动后重启进程树",
    Action = ScheduleTaskAction.RestartProcessTree,
    Trigger = ScheduleTriggerType.EveryStartupAfterDelay,
    DelaySeconds = 5,
    Enabled = true,
    Description = "每次启动管家后延迟5秒重启进程树"
};

rootProcessNode.ScheduleTaskConfigs.Add(taskConfig);
```

## 界面集成

### ScheduleViewModel 扩展

新增以下属性和命令：

```csharp
/// <summary>新的任务配置列表（推荐使用）</summary>
public ObservableCollection<ScheduleTaskConfig> ScheduleTaskConfigs { get; set; }

/// <summary>触发器类型选项</summary>
public List<string> TriggerTypeOptions { get; }

/// <summary>任务操作类型选项</summary>
public ObservableCollection<string> TaskActionTypes { get; set; }

/// <summary>添加新的任务配置命令</summary>
public ReactiveCommand<Unit, Unit> AddTaskConfigCommand { get; }
```

### XML序列化

新的任务配置会自动序列化到配置文件中：

```xml
<ProcessItem>
  <MetaData Name="进程1" Path="..." />
  <ScheduleTaskConfig 
      Name="定时截图" 
      Action="TakeScreenshot" 
      Trigger="IntervalAfterStartup" 
      DelaySeconds="5" 
      Enabled="true" />
</ProcessItem>
```

## 向后兼容性

系统保留了旧的 `ScheduleItem` 类和相关逻辑，确保现有配置文件仍能正常加载和运行。新的任务配置与旧的任务项并行工作，互不影响。

## 故障排除

### 任务不执行

1. 检查任务是否已启用 (`Enabled = true`)
2. 检查日志输出，查看是否有错误信息
3. 验证任务配置是否正确，特别是触发器类型和参数
4. 确保管家进程正在运行

### 截图保存失败

1. 检查 `Screenshots` 文件夹是否存在且有写入权限
2. 检查磁盘空间是否足够
3. 查看日志中的错误信息

### 定时任务执行不准时

1. 系统时间是否正确设置
2. 是否有其他耗时任务阻塞了主线程
3. 间隔时间设置是否过短（建议不少于1秒）

## 文件结构

- `DaemonKit/Core/ScheduleTaskEngine.cs` - 任务调度引擎核心类
- `DaemonKit/ViewModels/ScheduleViewModel.cs` - 视图模型（已扩展）
- `DaemonKit/Core/ProcessItem.cs` - 进程项类（已扩展）
- `DaemonKit/Converters/ScheduleTaskConverters.cs` - UI转换器
- `DaemonKit/MainWindow.xaml.cs` - 主窗口（集成任务引擎）

## 更新记录

### v2.0.0 - 新的任务调度系统

- ✅ 添加4种新的触发器类型
- ✅ 添加7种新的任务操作类型
- ✅ 实现完整的任务调度引擎
- ✅ 支持灵活的任务配置和管理
- ✅ 保持与旧版本的兼容性
- ✅ 添加事件通知机制

---

**最后更新**: 2026年1月4日
