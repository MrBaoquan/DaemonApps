# DaemonKit 任务计划 - 快速参考指南

## 🚀 快速开始

### 创建并执行一个任务

```csharp
// 创建任务配置
var task = new ScheduleTaskConfig
{
    Name = "每日关机",
    Action = ScheduleTaskAction.ShutdownSystem,
    Trigger = ScheduleTriggerType.Daily,
    DailyTime = "16:30:00",
    Enabled = true
};

// 添加到进程节点
rootNode.ScheduleTaskConfigs.Add(task);

// 保存配置
saveConfig();
```

---

## 📋 触发器类型对照表

### 1️⃣ Daily（每天指定时间）
```csharp
Trigger = ScheduleTriggerType.Daily,
DailyTime = "16:30:00"  // 必需：HH:mm:ss格式
```
**执行**: 每天16:30执行一次

### 2️⃣ OncePerDayAfterStart（每天首次启动后延迟）
```csharp
Trigger = ScheduleTriggerType.OncePerDayAfterStart,
DelaySeconds = 10  // 延迟10秒后执行
```
**执行**: 每天首次启动管家→延迟10秒→执行

### 3️⃣ EveryStartupAfterDelay（每次启动后延迟）
```csharp
Trigger = ScheduleTriggerType.EveryStartupAfterDelay,
DelaySeconds = 5  // 每次启动都延迟5秒
```
**执行**: 每次启动管家→延迟5秒→执行（每次启动都会执行）

### 4️⃣ IntervalAfterStartup（启动后周期循环）
```csharp
Trigger = ScheduleTriggerType.IntervalAfterStartup,
DelaySeconds = 60  // 每隔60秒执行一次
```
**执行**: 启动后→每隔60秒执行一次→直到管家关闭

---

## 🎯 操作类型对照表

| 操作类型 | 代码 | 说明 |
|---------|------|------|
| 启动进程 | `StartProcess` | 启动目标进程 |
| 停止进程 | `StopProcess` | 停止目标进程 |
| 重启进程 | `RestartProcess` | 停止后重启进程 |
| 重启进程树 | `RestartProcessTree` | 重启根节点下所有进程 |
| 关闭电脑 | `ShutdownSystem` | 执行系统关闭 |
| 重启电脑 | `RestartSystem` | 执行系统重启 |
| 全屏截图 | `TakeScreenshot` | 截图保存到Screenshots文件夹 |

---

## 💡 常见配置示例

### 示例1: 每天16:30关闭电脑
```csharp
var task = new ScheduleTaskConfig
{
    Name = "每日16:30关机",
    Action = ScheduleTaskAction.ShutdownSystem,
    Trigger = ScheduleTriggerType.Daily,
    DailyTime = "16:30:00",
    Enabled = true
};
rootNode.ScheduleTaskConfigs.Add(task);
```

### 示例2: 启动后延迟10秒重启电脑
```csharp
var task = new ScheduleTaskConfig
{
    Name = "启动后10秒重启",
    Action = ScheduleTaskAction.RestartSystem,
    Trigger = ScheduleTriggerType.OncePerDayAfterStart,
    DelaySeconds = 10,
    Enabled = true
};
rootNode.ScheduleTaskConfigs.Add(task);
```

### 示例3: 启动后5秒重启进程树
```csharp
var task = new ScheduleTaskConfig
{
    Name = "启动后重启进程树",
    Action = ScheduleTaskAction.RestartProcessTree,
    Trigger = ScheduleTriggerType.EveryStartupAfterDelay,
    DelaySeconds = 5,
    Enabled = true
};
rootNode.ScheduleTaskConfigs.Add(task);
```

### 示例4: 每隔5秒自动截图
```csharp
var task = new ScheduleTaskConfig
{
    Name = "定时截图(5秒)",
    Action = ScheduleTaskAction.TakeScreenshot,
    Trigger = ScheduleTriggerType.IntervalAfterStartup,
    DelaySeconds = 5,
    Enabled = true,
    Description = "启动后每隔5秒截图一次"
};
rootNode.ScheduleTaskConfigs.Add(task);
```

### 示例5: 每隔60秒重启一个进程
```csharp
var task = new ScheduleTaskConfig
{
    Name = "定时重启进程(60秒)",
    Action = ScheduleTaskAction.RestartProcess,
    Trigger = ScheduleTriggerType.IntervalAfterStartup,
    DelaySeconds = 60,
    Enabled = true
};
childNode.ScheduleTaskConfigs.Add(task);
```

---

## 🔧 关键类和方法

### ScheduleTaskConfig
```csharp
public class ScheduleTaskConfig
{
    public string Name { get; set; }                    // 任务名称
    public ScheduleTaskAction Action { get; set; }      // 操作类型
    public ScheduleTriggerType Trigger { get; set; }    // 触发器类型
    public string DailyTime { get; set; }               // 每日时间(HH:mm:ss)
    public int DelaySeconds { get; set; }               // 延迟/间隔(秒)
    public bool Enabled { get; set; }                   // 是否启用
    public string Description { get; set; }             // 描述
}
```

### ScheduleTaskEngine
```csharp
public class ScheduleTaskEngine
{
    // 检查并执行待执行的任务
    public void CheckAndExecutePendingTasks()
    
    // 任务执行前事件
    public event EventHandler<ScheduleTaskContext> TaskExecuting
    
    // 任务执行后事件
    public event EventHandler<ScheduleTaskContext> TaskExecuted
    
    // 清理资源
    public void Dispose()
}
```

### ProcessItem
```csharp
public class ProcessItem
{
    // 新的任务配置列表
    public List<ScheduleTaskConfig> ScheduleTaskConfigs { get; set; }
}
```

---

## 📁 文件位置

| 内容 | 位置 |
|-----|------|
| 任务调度引擎 | `Core/ScheduleTaskEngine.cs` |
| 视图模型 | `ViewModels/ScheduleViewModel.cs` |
| UI转换器 | `Converters/ScheduleTaskConverters.cs` |
| 进程类 | `Core/ProcessItem.cs` |
| 截图存储 | `{AppRoot}/Screenshots/` |
| 配置文件 | `{AppData}/DaemonKit/config.xml` |
| 日志文件 | `{AppRoot}/Logs/DaemonKit.log` |

---

## 🐛 常见问题

### Q: 任务没有执行？
**A:** 
1. 检查 `Enabled = true`
2. 检查日志文件看是否有错误
3. 确保DaemonKit进程正在运行
4. 验证系统时间是否正确

### Q: 截图在哪里保存？
**A:** 自动保存到 `{DaemonKit根目录}/Screenshots/` 文件夹，文件名格式为 `screenshot_YYYY-MM-DD_HH-mm-ss.png`

### Q: 如何禁用某个任务？
**A:** 设置 `Enabled = false`，或通过UI界面的开关禁用

### Q: 支持多少个任务？
**A:** 理论上无限制，受系统资源限制。建议不超过100个任务。

### Q: 任务会不会重复执行？
**A:** 不会。引擎内部追踪每个任务的最后执行时间，确保不重复执行。

---

## ⚙️ 最佳实践

### ✅ DO
- 为任务起有意义的名字
- 为复杂任务添加描述信息
- 测试任务前启用日志
- 定期检查日志输出

### ❌ DON'T
- 设置过短的间隔时间（< 1秒）
- 在截图任务中使用极短间隔（会消耗大量磁盘空间）
- 关闭电脑任务与其他任务冲突配置
- 忘记保存配置

---

## 📊 性能建议

| 场景 | 建议 |
|-----|------|
| 每日定时任务 | 最多20个 |
| 周期性任务 | 最少间隔10秒 |
| 截图任务 | 最少间隔30秒|
| 系统重启任务 | 仅设置1-2个 |

---

## 🔗 相关文档

- [任务计划功能重构说明](任务计划功能重构说明.md) - 详细技术文档
- [任务计划重构完成总结](任务计划重构完成总结.md) - 重构总结报告

---

**最后更新**: 2026年1月4日  
**版本**: 2.0.0
