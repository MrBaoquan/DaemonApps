# DaemonKit 命令控制使用文档

## 📖 概述

DaemonKit 提供了基于 UDP 的远程命令控制功能，允许通过网络发送命令来控制系统和进程树的运行状态。

---

## 🔌 通信端口

| 端口 | 用途 | 说明 |
|------|------|------|
| **7007** | 元数据广播 | DaemonKit 每 3 秒广播一次机器信息（IP、CPU、GPU、内存等） |
| **7008** | 命令控制 | 接收系统控制命令（重启、关机、进程树控制等） |
| **7777** | 心跳监听 | 接收进程心跳数据，用于进程健康监测 |

---

## 📝 命令格式

### JSON 格式规范

```json
{
  "evt": <命令ID>,
  "data": {
    // 可选的命令数据
  }
}
```

### 字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `evt` | int | ✅ | 命令事件 ID |
| `data` | object | ❌ | 命令附加数据（仅部分命令需要） |

---

## 🎯 支持的命令

### 1. 系统关机 (SHUTDOWN)
- **命令 ID:** `1001`
- **功能:** 立即关闭系统
- **端口:** 7008
- **格式:** `{"evt": 1001}`

### 2. 系统重启 (RESTART)
- **命令 ID:** `1002`
- **功能:** 立即重启系统
- **端口:** 7008
- **格式:** `{"evt": 1002}`

### 3. 系统启动 (BOOT)
- **命令 ID:** `1003`
- **功能:** 启动系统（预留，未实现）
- **状态:** ⚠️ 已定义但未实现
- **说明:** 此命令 ID 已保留，可用于实现网络唤醒 (Wake-on-LAN) 功能

### 4. 重启进程树 (RESTART_NODE_TREE)
- **命令 ID:** `1004`
- **功能:** 重新启动整个进程树
- **端口:** 7008
- **格式:** `{"evt": 1004}`

### 5. 停止进程树 (STOP)
- **命令 ID:** `1005`
- **功能:** 停止整个进程树的运行
- **端口:** 7008
- **格式:** `{"evt": 1005}`

### 6. 进程心跳 (HEARTBEAT)
- **命令 ID:** `1221`
- **功能:** 向 DaemonKit 报告进程存活状态
- **端口:** `7777` ⚠️ 注意：不是 7008
- **格式:** `{"evt": 1221, "data": {"process": "C:/MyApp/MyProcess.exe"}}`
- **建议间隔:** 每 5-30 秒发送一次
- **说明:** `data.process` 字段必须包含进程的完整路径，需与进程树中配置的路径一致

---

## � 设备信息广播

DaemonKit 每 3 秒通过 UDP 广播端口 **7007** 发送机器信息。

**广播数据包含:**
- 机器名称
- IP 地址列表
- CPU 信息
- GPU 信息
- 内存信息

---

## 🔒 安全特性

### 当前实现
- ✅ **空值检查:** 所有命令数据经过严格的空值校验
- ✅ **异常恢复:** 单次命令失败不影响后续命令接收
- ✅ **日志审计:** 所有命令执行都有完整日志记录

### 安全建议

⚠️ **重要提示:** 当前版本不包含身份验证机制，建议：

1. **网络隔离:** 仅在受信任的内部网络中使用
2. **防火墙规则:** 限制端口 7007、7008、7777 的访问来源
3. **监控日志:** 定期检查日志文件（`Logs/` 目录）中的异常命令

---

## 🛠️ 常见问题

### Q1: 发送命令后没有反应？
**检查项:**
1. 确认目标机器 IP 地址正确
2. 确认端口未被防火墙阻止
3. 检查 DaemonKit 日志文件（`Logs/` 目录）
4. 确认命令格式正确（JSON 格式，`evt` 字段存在）

### Q2: 心跳命令不工作？
**常见错误:**
- ❌ 发送到错误的端口（应该是 7777 而不是 7008）
- ❌ 进程路径与配置不匹配
- ❌ 缺少 `data` 字段或 `data.process` 字段

### Q3: 命令执行有延迟吗？
- **命令接收:** 实时（UDP 延迟 < 1ms）
- **命令处理:** 立即在主线程执行
- **系统响应:** 取决于具体命令

---

## 📝 命令快速参考

| 命令 | ID | 端口 | 格式 | 说明 |
|------|-----|------|------|------|
| 关机 | 1001 | 7008 | `{"evt":1001}` | 立即关闭系统 |
| 重启 | 1002 | 7008 | `{"evt":1002}` | 立即重启系统 |
| 启动 | 1003 | 7008 | `{"evt":1003}` | ⚠️ 未实现 |
| 重启进程树 | 1004 | 7008 | `{"evt":1004}` | 重启所有托管进程 |
| 停止进程树 | 1005 | 7008 | `{"evt":1005}` | 停止所有托管进程 |
| 心跳 | 1221 | 7777 | `{"evt":1221,"data":{"process":"..."}}` | 进程健康检查 |

---

## 📞 技术支持

- **日志文件:** `<DaemonKit目录>/Logs/`
- **配置文件:** `<DaemonKit目录>/Config/`
- **源代码:**
  - 命令定义: `DaemonKit/Core/Types.cs`
  - 命令处理: `DaemonKit/MainWindow.xaml.cs` (第 473-540 行)

---

## 📄 版本信息

- **文档版本:** 1.0
- **适用版本:** DaemonKit v1.x
- **最后更新:** 2025-11-18

---

## ⚖️ 许可声明

本文档基于 DaemonKit 项目编写，仅供内部使用和参考。
